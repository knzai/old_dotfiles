gitup() { 
  if git update-index --refresh; then
    stashed=
  else
    stashed=1
    git stash save 'update temp'
  fi
  git fetch;
  git rebase -v origin/master;
  if [ -n "$stashed" ]; then
     git stash pop
  fi
}